# CrowdSec — Log acquisition config for KoraSpace
# Copy to: /etc/crowdsec/acquis.d/koraspace.yaml

---
filenames:
  - /var/log/nginx/access.log
  - /var/log/nginx/error.log
labels:
  type: nginx
---
filenames:
  - /var/log/auth.log
  - /var/log/syslog
labels:
  type: syslog
